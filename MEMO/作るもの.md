# BLE通信を使った手紙バケツリレー

## コンセプト
「すれ違い通信」という技術的制約を逆手に取り、デジタルでありながら「物理的な距離と時間」を感じさせるメッセージング体験を構築する。AさんからBさんへの手紙が、見知らぬ誰かのデバイスを経由して届く「一機一会」の物語を可視化する。
手紙はウイルスのように感染しながら広がる。すれ違った人全員が配達員になり、誰かが宛先とすれ違ったときに手紙の拡散は終わる。差出人は手紙を出した後、届いたのかもどんな経路をたどったかも知ることができない。それがこのアプリのいいところ？である。

---

## ターゲット
SNS時代に、あえて連絡を「困難」にして楽しみたい人。既読、通知、返信ストレスから解放されたい人。

---

## 機能一覧
### ユーザー登録（初回起動時のみ）
- ユーザー名（本名推奨）を登録する。変更は今回は考えてない。
- BLE権限・位置情報権限に同意する。
- 次回以降はデバイスにセッションが保存されており、ログイン操作不要で起動する。
- 同性同名は今回はスルー

### ホーム画面
- ポストのイラスト？表示されている。
- 自分あての手紙が届いている場合、ポストに手紙が刺さっているビジュアルになる。
- ポストをタップ又は受信ボタンをクリックすると自分あてに届いた手紙を一覧画面に遷移する。

### 手紙作成
- 宛先(本名)と本文(テキストのみ)を入力する。
- 一度に編集できる下書きは1通のみ。
- 戻るボタンを押すと、「下書き保持」か「削除」かを選べる。

### 投函
- 投函ボタンを押すと、現在地から1km以内(仮)の実在するポスト（郵便ポスト）が地図上で表示される。
- ポストを選択すると確認ダイアログが出る。
- 投函確定すると：
  - 手紙データがサーバーに登録される。
  - 手紙データの木構造に差出人がルートノードとして登録される。
  - 差出人自身の端末からは手紙の内容が見えなくなる。（送ったら最後何もわからないくなる。）

### BLEすれ違い処理
- アプリはバックグラウンドでも常時BLEをスキャンしている。
- すれ違いを検知したとき：
  1. BLEでお互いのユーザー名を交換する。
  2. 直近の重複すれ違いをチェックし、一定時間以内なら処理を中断する。（これで1日とかに設定したら1日一回のすれ違いにできるかも）
  3. すれ違いをサーバーに記録する。
  4. 相手が運搬中ので、まだ宛先に届いていない手紙を取得する。
  5. 各手紙に対して以下を実行する。
   - 手紙を自分の運搬リストに追加する。
   - すれ違い位置をサーバーに保持する。
   - 手紙の木構造に新しいノードを追加する。
   - 自分が宛先があれば手紙を「到達済み」にする。
- 一度渡した相手には同じ手紙を再度渡さない（木構造に相手のノードが存在するかで判定）。

### 運んでいる手紙一覧
- 現在自分が運んでいる手紙の一覧が表示される。（差出人、宛先のみ見える、手紙の内容は見れない）
- 手紙を選択すると、その手紙の経路をリアルタイムで地図上に表示する。
  - 各ノード（すれ違いポイント）がピンで表示される。
  - 自分が経由したノードは強調色で表示させる。（わかりやすくするため）
  - ノード感が線で結ばれ、経路が視覚的にわかる。

### 届いた手紙一覧
- 自分宛に届いた手紙の一覧
- 手紙を開くと本文と自分に届くまでの経路（誰を経路してきたか）が見られる。
- 差出人には通知が届かないため、気軽に読める。


## データ設計（Firestore）
 
```mermaid
erDiagram
 
    USERS {
        string user_name PK "ユーザー名"
        list carrying_letter_ids "運搬中の手紙IDリスト"
    }
 
    LETTERS {
        string letter_id PK "手紙ID"
        string to_user FK "宛先ユーザー名"
        string from_user FK "差出人ユーザー名"
        string sentence "本文"
        boolean is_survival "未到達=true"
        json tree "木構造（nodes / edges）"
    }
 
    LOCATIONS {
        string location_id PK "位置記録ID"
        string letter_id FK "手紙ID"
        string user_name FK "ユーザー名"
        double latitude "緯度"
        double longitude "経度"
        long timestamp "記録時刻"
    }
 
    ENCOUNTERS {
        string encounter_id PK "すれ違いID"
        string userA "ユーザーA"
        string userB "ユーザーB"
        long timestamp "すれ違い時刻"
    }
 
    USERS ||--o{ LETTERS : "差出（from_user）"
    USERS ||--o{ LETTERS : "宛先（to_user）"
    USERS ||--o{ LOCATIONS : "records"
    LETTERS ||--o{ LOCATIONS : "has"
```

### 各フィールドの補足
 
**USERS**
- `carrying_letter_ids` — 自分が今運んでいる手紙のIDリスト。すれ違い時に追加される。これがないと「運搬中の手紙一覧」を全LETTERSから検索しないといけなくなる。
**LETTERS**
- `is_survival` — まだ宛先に届いていない手紙かどうか。`false`になると他のユーザーへの感染が止まる。
---

## 関数一覧
### ViewModel
#### RegisterViewModel
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `onStartClicked()` | なし | なし | スタートボタン押下。名前入力UIを表示する |
| `onNameChanged(text)` | ユーザー名 | なし | 名前入力フィールドの変更を受け取り、状態を更新する |
| `onNameSubmitClicked()` | ユーザー名 | なし | `UserRepository.saveUser()`を呼んでユーザーテーブルにユーザー名を保存する |
| `onPermissionResult(granted)` | 許可したか | なし | 権限許可の結果を受け取り、許可済みなら`BleRepository.startBle()`を呼ぶ |

#### HomeViewModel
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `onReceivedClicked()` | なし | なし | 届いた手紙一覧画面へ遷移 |
| `onCarryingClicked()` | なし | なし | 運搬中手紙一覧画面へ遷移 |
| `onCreateLetterClicked()` | なし | なし | 手紙作成画面へ遷移 |

#### EditLetterViewModel
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `onCreateLetter()` | なし | なし | ローカルに下書きが保存されていれば読み込み、なければ空白で初期化する |
| `onToChanged(text)` | 宛先名 | なし | 宛先入力の変更を受け取り、状態を更新する |
| `onSentenceChanged(text)` | 本文 | なし | 本文入力の変更を受け取り、状態を更新する |
| `onSaveDraftClicked()` | 内容 | なし | `DraftRepository.saveDraft()`を呼んで下書きをローカルに保存する |
| `onSelectPostClicked()` | 現在位置（緯度・経度） | ポスト座標リスト | 現在位置を取得し、`PostRepository.getNearbyPosts()`を呼んで周辺のポスト一覧を取得する |
| `onPostSelected(post)` | 選択したポスト | なし | 選択されたポストを状態に反映する |
| `onSubmitClicked()` | ユーザー名 | なし | `LetterRepository.sendLetter()`でletterに手紙を登録し、`LocationRepository.saveLocation()`でlocationに位置を登録する |

#### ReceivedViewModel
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `loadReceivedLetters()` | ユーザー名 | 受信手紙ID一覧 | `LOCATIONS`をユーザー名で絞り、`letter_id`と`LETTERS`を結び付けて宛先が自分のものを返す |
| `onLetterClicked(letterId)` | 手紙ID | なし | 手紙選択。`loadLetterDetail()`を呼ぶ |
| `loadLetterDetail(letterId)` | 手紙ID | 手紙詳細 | `LETTERS`を`letter_id`で絞って宛先・差出人・内容・木を返す |

#### CarryViewModel
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `loadCarryingLetters()` | ユーザー名 | 運搬手紙一覧 | `LOCATIONS`をユーザー名で絞り、`letter_id`を返す |
| `onLetterClicked(letterId)` | 手紙ID | なし | 手紙選択。`loadLetterDetail()`を呼ぶ |
| `loadLetterDetail(letterId)` | 手紙ID | 地図（Tree） | `LETTERS`を`letter_id`で絞り、木のノード全てに対してユーザー名と`letter_id`で緯度経度を取得し、`BuildRouteTreeUseCase.buildTree()`で木として返す |
 
---

### UseCase
#### RelayLetterUseCase
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `execute(myUserName, targetUserName)` | 自分・相手のユーザー名 | なし | すれ違い検知時に呼ばれる。重複チェック・手紙取得・位置保存・tree更新・宛先判定を一括で行う |

#### BuildRouteTreeUseCase
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `buildTree(locations)` | 位置履歴一覧 | Tree | `LOCATIONS`の一覧からノードとエッジを生成し、地図表示用の`Tree`を返す |
 
---

### Repository
#### UserRepository
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `saveUser(userName)` | ユーザー名 | なし | ユーザー名をFirestoreのUSERSに保存する |
| `getUser()` | なし | User | 保存済みのユーザー情報を取得する |

#### LetterRepository
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `sendLetter(letter)` | 手紙データ | なし | 手紙をFirestoreのLETTERSに新規登録する |
| `getReceivedLetters(userName)` | ユーザー名 | 受信手紙ID一覧 | `to_user=自分`かつ`is_survival=false`の手紙一覧を取得する |
| `getCarryingLetters(userName)` | ユーザー名 | 運搬手紙一覧 | 自分が運搬中の手紙一覧を取得する |
| `getLetter(letterId)` | 手紙ID | 手紙詳細 | 指定IDの手紙詳細を取得する |
| `getCarriedLetters(userName)` | ユーザー名 | 手紙一覧 | 相手が運搬中（`is_survival=true`）の手紙一覧を取得する（すれ違い処理用） |
| `updateSurvival(letterId, isAlive)` | 手紙ID・bool | なし | 手紙の`is_survival`を更新する |

#### LocationRepository
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `saveLocation(location)` | 位置情報 | なし | すれ違い時の位置情報をFirestoreのLOCATIONSに保存する |
| `getLocationsByLetter(letterId)` | 手紙ID | 位置履歴一覧 | 指定手紙の位置履歴一覧を取得する |

#### DraftRepository
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `saveDraft(draft)` | 内容 | なし | 下書きをローカルに保存する |
| `loadDraft()` | なし | 下書き | 保存済みの下書きを取得する |
| `clearDraft()` | なし | なし | 下書きを削除する |

#### PostRepository
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `getNearbyPosts(lat, lon)` | 現在位置（緯度・経度） | ポスト座標リスト | 現在位置周辺のポスト一覧を外部APIから取得する |

#### BleRepository
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `startBle()` | なし | なし | BLEのスキャン・アドバタイズを開始する |
| `onEncounter(callback)` | なし | なし | すれ違いを検知したときのコールバックを登録する |

#### EncounterRepository
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `getLastEncounter(userA, userB)` | ユーザーA・B | Encounter | 同一ユーザーとの直近すれ違いを取得する（重複チェック用） |
| `saveEncounter(encounter)` | すれ違い記録 | なし | すれ違い記録をFirestoreのENCOUNTERSに保存する |

#### TreeRepository
| 関数 | 入力 | 出力 | 内容 |
|---|---|---|---|
| `addNode(letterId, parentUser, newUser, location)` | 手紙ID・親ユーザー・新ユーザー・位置 | なし | 手紙の木構造に新しいノードとエッジを追加する |
 
---


## フォルダ構成
 
```
letterble/
├── data/
│   ├── datasource/
│   │   ├── ble/
│   │   │   ├── BleAdvertiser.kt       # BLEアドバタイズ（自分のuser_nameを周囲に発信）
│   │   │   ├── BleManager.kt          # BLE全体の管理・初期化
│   │   │   └── BleScanner.kt          # BLEスキャン（周囲のデバイスを検知）
│   │   ├── firestore/
│   │   │   ├── EncounterFirestoreDataSource.kt  # ENCOUNTERSコレクションの読み書き
│   │   │   ├── LetterFirestoreDataSource.kt     # LETTERSコレクションの読み書き
│   │   │   ├── LocationFirestoreDataSource.kt   # LOCATIONSコレクションの読み書き
│   │   │   ├── TreeFirestoreDataSource.kt       # LETTERSのtreeフィールドの読み書き
│   │   │   └── UserFirestoreDataSource.kt       # USERSコレクションの読み書き
│   │   └── local/
│   │       └── DraftLocalDataSource.kt          # 下書きのローカル保存（SharedPreferences等）
│   └── repository/
│       ├── BleRepository.kt           # BLEのスキャン・すれ違い検知を提供
│       ├── DraftRepository.kt         # 下書きの保存・取得・削除を提供
│       ├── EncounterRepository.kt     # すれ違い記録の保存・重複チェックを提供
│       ├── LetterRepository.kt        # 手紙の送信・取得・状態更新を提供
│       ├── LocationRepository.kt      # 位置情報の保存・取得を提供
│       ├── PostRepository.kt          # 近くのポスト一覧を外部APIから取得
│       ├── TreeRepository.kt          # 手紙の木構造へのノード追加を提供
│       └── UserRepository.kt          # ユーザー情報の保存・取得を提供
├── domain/
│   ├── model/
│   │   ├── Edge.kt        # 木構造のエッジ（fromNodeId → toNodeId）
│   │   ├── Encounter.kt   # すれ違い記録（userA, userB, timestamp）
│   │   ├── Letter.kt      # 手紙データ（letter_id, from_user, to_user, sentence, is_survival, tree）
│   │   ├── Location.kt    # 位置情報（letter_id, user_name, latitude, longitude, timestamp）
│   │   ├── Node.kt        # 木構造のノード（id, userName, latitude, longitude）
│   │   ├── Post.kt        # 郵便ポスト（id, name, latitude, longitude） ※要追加
│   │   ├── Tree.kt        # 木構造全体（nodes, edges）
│   │   └── User.kt        # ユーザー（user_name, carrying_letter_ids）
│   └── usecase/
│       ├── BuildRouteTreeUseCase.kt   # 位置履歴からTree（nodes/edges）を生成する
│       └── RelayLetterUseCase.kt      # すれ違い時の手紙リレー処理をまとめたコアロジック
├── feature/
│   ├── carry/
│   │   ├── CarryDetailScreen.kt   # 運搬中手紙の詳細表示（from_user, to_user）
│   │   ├── CarryMapScreen.kt      # 運搬中手紙の経路をツリーで地図表示
│   │   ├── CarryScreen.kt         # 運搬中手紙の一覧表示
│   │   └── CarryViewModel.kt      # 運搬画面の状態管理
│   ├── edit_letter/
│   │   ├── EditLetterScreen.kt    # 手紙作成画面（宛先・本文入力）
│   │   ├── EditLetterViewModel.kt # 手紙作成の状態管理・下書き・投函処理
│   │   └── PostSelectScreen.kt    # 近くのポストを地図上に表示して選択する画面
│   ├── home/
│   │   ├── HomeScreen.kt          # ホーム画面（ポストのイラスト・各画面への導線）
│   │   └── HomeViewModel.kt       # ホーム画面の状態管理・ナビゲーション
│   ├── received/
│   │   ├── ReceivedDetailScreen.kt  # 届いた手紙の詳細表示（本文・経路）
│   │   ├── ReceivedScreen.kt        # 届いた手紙の一覧表示
│   │   ├── ReceivedViewModel.kt     # 受信画面の状態管理
│   │   └── RouteMapScreen.kt        # 手紙が届くまでの経路を地図表示
│   └── register/
│       ├── RegisterScreen.kt      # 初回起動・ユーザー名登録・権限許可画面
│       └── RegisterViewModel.kt   # 登録画面の状態管理・BLE開始処理
├── navigation/
│   ├── AppNavGraph.kt    # 画面遷移のルーティング定義
│   └── Destinations.kt   # 各画面のルート名を定数で管理
└── ui/
    ├── components/
    │   ├── CommonButton.kt  # アプリ共通のボタンコンポーネント
    │   └── MapView.kt       # 地図表示の共通コンポーネント
    └── theme/
        ├── Color.kt   # アプリ全体のカラー定義
        ├── Theme.kt   # Material Themeの設定
        └── Type.kt    # フォント・テキストスタイルの定義
```
 
---

## 技術スタック
 
| 項目 | 内容 |
|---|---|
| プラットフォーム | Android |
| 言語 | Kotlin |
| UI | Jetpack Compose |
| バックエンド | Firebase Firestore（リアルタイム更新） |
| 通知 | Firebase Cloud Messaging（FCM） |
| 地図 | 未定（Google Maps SDK or OSMDroid） |
| BLE | Android BLE API（バックグラウンド常時動作） |
| ポストデータ | 実在の郵便ポストをGPS座標で管理 |
 
---

## 制約・仕様メモ
 
- 手紙は永遠に漂い続ける（有効期限なし、アンインストールされても消えない）。
- 宛先に届いても他のキャリアからデータは消えない（`is_survival` が false になるが、他ユーザーのコピーは残る）。
- 差出人は手紙が届いたことも経路も知ることができない。
- 同姓同名は今回スコープ外。
- ハッカソン向け（1週間半、5人チーム：デザイン2名・開発3名）。
- iPhoneメンバーがいるためエミュレータでの動作確認を考慮する。